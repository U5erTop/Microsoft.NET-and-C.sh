## Темы заданий:
1. Обзор методов.
2. Различия между процедурами и функциями.
3. Работа методов - примеры.
4. Управляющая структура return.
5. Использование сторожевых операторов (Guard Clauses).
6. Сторожевые операторы для защиты номинального варианта.
7. Использование ref и out аргументов в методах.
---
## Список задач 

### 1. Базовые методы (Математика и логика)
1. Напишите метод, принимающий два числа и возвращающий их сумму.
2. Создайте метод, возвращающий разность двух чисел.
3. Напишите метод для умножения двух чисел.
4. Реализуйте метод деления одного числа на другое (возвращает double).
5. Напишите метод, возвращающий квадрат переданного числа.
6. Создайте метод, возвращающий куб переданного числа.
7. Реализуйте метод, определяющий, является ли число четным (возвращает bool).
8. Напишите метод, определяющий, является ли число нечетным.
9. Создайте метод для нахождения максимума из двух чисел.
10. Создайте метод для нахождения минимума из двух чисел.
11. Напишите метод, вычисляющий модуль числа (Abs).
12. Реализуйте метод для расчета площади круга по радиусу.
13. Напишите метод, определяющий, является ли число простым.
14. Создайте метод, вычисляющий периметр прямоугольника.
15. Реализуйте метод перевода градусов Цельсия в Фаренгейты.
16. Реализуйте метод перевода Фаренгейтов в градусы Цельсия.
17. Напишите метод, возвращающий среднее арифметическое двух чисел.
18. Создайте метод для вычисления НОД (наибольшего общего делителя) двух чисел.
19. Напишите метод для вычисления НОК (наименьшего общего кратного).
20. Реализуйте метод, проверяющий, попадает ли число в заданный диапазон `[min, max]`.

### 2. Работа со строками
21. Напишите метод, возвращающий длину переданной строки.
22. Создайте метод, который переворачивает строку (задом наперед).
23. Реализуйте метод, проверяющий, является ли строка палиндромом.
24. Напишите метод для подсчета количества гласных букв в строке.
25. Создайте метод для подсчета количества согласных букв.
26. Реализуйте метод, переводящий строку в верхний регистр.
27. Напишите метод, переводящий строку в нижний регистр.
28. Создайте метод, делающий первую букву строки заглавной.
29. Реализуйте метод, удаляющий все пробелы из строки.
30. Напишите метод для подсчета количества слов в строке.
31. Создайте метод, проверяющий, начинается ли строка с заданной подстроки.
32. Реализуйте метод, проверяющий, заканчивается ли строка заданной подстрокой.
33. Напишите метод, заменяющий в строке один символ на другой.
34. Создайте метод, повторяющий строку `N` раз.
35. Реализуйте метод, удаляющий все цифры из строки.
36. Напишите метод, извлекающий все цифры из строки в новую строку.
37. Создайте метод для проверки, являются ли две строки анаграммами.
38. Напишите метод, находящий самое длинное слово в строке.
39. Напишите метод, находящий самое короткое слово в строке.
40. Реализуйте метод, маскирующий номер кредитной карты (оставляет только последние 4 цифры).

### 3. Массивы и коллекции
41. Напишите метод, вычисляющий сумму элементов массива.
42. Создайте метод для нахождения среднего значения массива.
43. Реализуйте метод поиска максимального элемента в массиве.
44. Напишите метод поиска минимального элемента в массиве.
45. Создайте метод, переворачивающий массив (reverse).
46. Реализуйте метод для сортировки массива по возрастанию.
47. Напишите метод для сортировки массива по убыванию.
48. Создайте метод, возвращающий индекс первого вхождения элемента в массиве.
49. Реализуйте метод, проверяющий наличие элемента в массиве.
50. Напишите метод для подсчета вхождений определенного элемента в массив.
51. Создайте метод, удаляющий дубликаты из массива (возвращает новый массив).
52. Реализуйте метод для объединения двух массивов в один.
53. Напишите метод, возвращающий только четные числа из массива.
54. Создайте метод, возвращающий только нечетные числа из массива.
55. Реализуйте метод циклического сдвига элементов массива влево.
56. Напишите метод циклического сдвига массива вправо.
57. Создайте метод проверки, отсортирован ли массив.
58. Реализуйте метод поиска второго по величине элемента массива.
59. Напишите метод поиска второго наименьшего элемента массива.
60. Создайте метод, умножающий все элементы массива на заданный множитель.

### 4. Перегрузка методов
61. Напишите перегруженные методы `Add` для сложения 2, 3 и 4 чисел.
62. Создайте перегрузки метода `Concat` для объединения 2 и 3 строк.
63. Реализуйте перегруженные методы `Area` для квадрата (1 параметр) и прямоугольника (2 параметра).
64. Напишите метод `Print`, перегруженный для типов int, double и string.
65. Создайте перегрузки метода `Multiply` для int, float и decimal.
66. Реализуйте метод `Max` для двух и трех чисел.
67. Напишите перегруженные методы `FormatDate` для разных форматов входных данных.
68. Создайте метод `Log`, перегруженный для простой строки и строки с уровнем важности.
69. Реализуйте перегрузки для создания массива: пустого заданного размера и заполненного дефолтным значением.
70. Напишите перегруженные методы `Parse` со значением по умолчанию в случае ошибки и без него.
71. Создайте метод `CalculateVolume` для куба и параллелепипеда.
72. Реализуйте перегрузки `DrawLine` по координатам точек и по длинам.
73. Напишите перегруженный метод поиска элемента (по значению и по предикату).
74. Создайте метод `GetGreeting`, перегруженный для имени и для имени с фамилией.
75. Реализуйте перегрузки метода `ReadData` из файла и из потока.
76. Напишите метод `ResizeArray`, принимающий новую длину или дельту изменения.
77. Создайте перегрузки `Round` до целого и до N знаков после запятой.
78. Реализуйте методы `Distance` для 2D (x,y) и 3D (x,y,z) точек.
79. Напишите перегруженные методы `Repeat` для символа и для строки.
80. Создайте перегрузку метода генерации случайного числа (от 0 до max и от min до max).

### 5. Ключевые слова ref, out и in
81. Напишите метод `Swap`, меняющий местами два числа через `ref`.
82. Реализуйте метод деления, возвращающий частное и остаток через `out`.
83. Создайте метод `TryParseInt`, возвращающий bool и распарсенное число через `out`.
84. Напишите метод, возвращающий одновременно `min` и `max` массива через `out`.
85. Реализуйте метод, увеличивающий переменную на 1 через `ref`.
86. Создайте метод, добавляющий постфикс к строке через `ref`.
87. Напишите метод, удваивающий значение переменной через `ref`.
88. Реализуйте метод поиска корней квадратного уравнения через `out`.
89. Создайте метод, разделяющий строку пополам и возвращающий две части через `out`.
90. Напишите метод для получения компонентов R, G, B из цвета (через `out`).
91. Реализуйте метод, принимающий структуру через модификатор `in` для оптимизации.
92. Создайте метод, устанавливающий 3 переменные в значения по умолчанию через `out`.
93. Напишите метод, извлекающий имя и фамилию из полного имени через `out`.
94. Реализуйте метод-калькулятор, возвращающий результат и флаг ошибки через `out`.
95. Создайте метод, сортирующий три переменные по возрастанию через `ref`.
96. Напишите метод чтения файла, возвращающий контент и статус операции (через `out`).
97. Реализуйте метод, обрезающий пробелы в строке, переданной по `ref`.
98. Создайте метод, получающий ширину и высоту консоли через `out`.
99. Напишите метод для парсинга URL на домен и путь (через `out`).
100. Реализуйте метод `Clamp`, ограничивающий значение по границам и меняющий его через `ref`.

### 6. Необязательные и именованные параметры
101. Напишите метод приветствия пользователя с необязательным параметром имени ("Гость").
102. Создайте метод конфигурации окна с необязательными шириной, высотой и заголовком.
103. Реализуйте метод отправки email с необязательными параметрами CC и BCC.
104. Напишите метод расчета цены товара с необязательной скидкой (по умолчанию 0).
105. Создайте метод подключения к БД, где порт по умолчанию равен 5432.
106. Реализуйте метод генерации пароля с необязательными флагами (цифры, спецсимволы).
107. Напишите метод форматирования текста с цветом по умолчанию (ConsoleColor.White).
108. Создайте метод создания пользователя со статусом "Active" по умолчанию.
109. Реализуйте метод пагинации `GetItems(int page, int pageSize = 10)`.
110. Напишите метод логгирования, где уровень лога по умолчанию `Info`.
111. Создайте метод расчета ИМТ (BMI) с использованием именованных аргументов при вызове.
112. Реализуйте метод рисования круга с координатами (0,0) по умолчанию.
113. Напишите метод экспорта данных в файл с расширением по умолчанию ".csv".
114. Создайте метод для воспроизведения звука с дефолтной громкостью 100.
115. Реализуйте метод выполнения HTTP-запроса с таймаутом по умолчанию 30 сек.
116. Напишите метод поиска файлов с необязательным фильтром "*.txt".
117. Создайте метод конвертации валют с валютой назначения по умолчанию "USD".
118. Реализуйте метод форматирования адреса с необязательным параметром квартиры.
119. Вызовите метод со множеством параметров, используя только именованные параметры.
120. Напишите метод, принимающий массив `params int[]` и необязательный множитель перед ним.

### 7. Рекурсивные методы
121. Напишите рекурсивный метод для вычисления факториала.
122. Создайте рекурсивный метод для нахождения N-го числа Фибоначчи.
123. Реализуйте рекурсивный бинарный поиск в массиве.
124. Напишите рекурсивный метод для нахождения суммы элементов массива.
125. Создайте рекурсивный метод для возведения числа в степень.
126. Реализуйте рекурсивный алгоритм "Ханойские башни".
127. Напишите рекурсивный алгоритм Евклида (НОД).
128. Создайте рекурсивный метод переворота строки.
129. Реализуйте рекурсивную проверку строки на палиндром.
130. Напишите рекурсивный метод подсчета количества цифр в числе.
131. Создайте рекурсивный метод суммы цифр числа.
132. Реализуйте рекурсивный метод вывода чисел от 1 до N.
133. Напишите рекурсивный метод вывода чисел от N до 1.
134. Создайте метод для рекурсивного обхода всех папок в директории.
135. Реализуйте рекурсивный метод вычисления функции Аккермана.
136. Напишите рекурсивный метод для генерации всех перестановок строки.
137. Создайте рекурсивный метод "сплющивания" (flatten) вложенных массивов.
138. Реализуйте рекурсивное вычисление гипотезы Коллатца.
139. Напишите рекурсивный метод перевода числа из десятичной системы в двоичную.
140. Создайте рекурсивный обход бинарного дерева (in-order).

### 8. Методы расширения (Extension Methods)
141. Напишите метод расширения для `string`, преобразующий её в `int`.
142. Создайте метод расширения для `int`, проверяющий его на четность (`IsEven()`).
143. Реализуйте метод расширения для `List<T>`, возвращающий случайный элемент.
144. Напишите метод расширения для `DateTime`, проверяющий, выходной ли это день.
145. Создайте метод расширения `ToCamelCase()` для `string`.
146. Реализуйте метод расширения для `string`, считающий количество слов.
147. Напишите метод расширения для `int` для генерации TimeSpan (например, `5.Days()`).
148. Создайте метод расширения для `double`, форматирующий его как валюту.
149. Реализуйте метод расширения `Truncate(length)` для усечения длинных строк.
150. Напишите метод расширения `ClearAll()` для любого массива.
151. Создайте метод расширения для `IEnumerable<T>`, удаляющий `null` элементы.
152. Реализуйте метод расширения `ToJson()` для любого объекта (`object`).
153. Напишите метод расширения `IsNumeric()` для строки.
154. Создайте метод расширения `IsPrime()` для целочисленного типа.
155. Реализуйте метод расширения `ToEpochTime()` для `DateTime`.
156. Напишите метод расширения `ReverseString()` прямо для класса `string`.
157. Создайте метод расширения `ContainsAll(...)` для коллекций.
158. Реализуйте метод расширения для `Enum`, чтобы получать атрибут `Description`.
159. Напишите метод расширения для `TimeSpan`, возвращающий время прописью.
160. Создайте метод расширения `Shuffle()` для перемешивания элементов `IList<T>`.

### 9. Кортежи (Tuples) как возвращаемые значения
161. Напишите метод, возвращающий минимальное и максимальное значение массива в виде кортежа `(int Min, int Max)`.
162. Создайте метод деления, возвращающий `(int Quotient, int Remainder)`.
163. Реализуйте метод парсинга строки, возвращающий `(bool Success, int Result)`.
164. Напишите метод, возвращающий Имя, Фамилию и Возраст человека в одном кортеже.
165. Создайте метод для получения X и Y координат мыши `(int x, int y)`.
166. Реализуйте метод, возвращающий площадь и периметр прямоугольника в виде кортежа.
167. Напишите метод поиска самого длинного и самого короткого слова в тексте `(string Longest, string Shortest)`.
168. Создайте метод чтения файла, возвращающий `(string Content, long SizeInBytes)`.
169. Реализуйте метод, возвращающий HTTP статус код и тело ответа.
170. Напишите метод, разделяющий массив на два: четные и нечетные числа `(int[] Evens, int[] Odds)`.
171. Создайте метод, возвращающий сумму, среднее и количество элементов списка.
172. Реализуйте метод, возвращающий день, месяц и год из строки с датой.
173. Напишите метод получения текущих широты и долготы пользователя.
174. Создайте метод разложения цвета на `(byte R, byte G, byte B, byte A)`.
175. Реализуйте метод извлечения протокола, домена и порта из URL с помощью кортежа.
176. Напишите метод для получения двух самых больших значений массива.
177. Создайте метод, возвращающий `(TimeSpan Start, TimeSpan End)` для смены работника.
178. Реализуйте метод вычисления скидки, возвращающий `(decimal OriginalPrice, decimal DiscountedPrice)`.
179. Напишите метод валидации пароля, возвращающий `(bool IsValid, string ErrorMessage)`.
180. Создайте метод обмена значениями двух переменных с использованием деконструкции кортежей `(a, b) = (b, a)`.

### 10. Локальные функции, Guard Clauses и безопасный код
181. Напишите метод с Guard Clause для проверки того, что строка не `null` и не пуста.
182. Создайте метод с Guard Clause, выбрасывающим исключение, если число отрицательное.
183. Реализуйте метод вычисления факториала, в котором сама рекурсия скрыта в локальной функции.
184. Напишите метод валидации Email с помощью регулярного выражения и Guard Clauses.
185. Создайте метод для работы с массивом, предварительно проверив индекс на выход за границы.
186. Реализуйте безопасное деление, проверяющее знаменатель на 0 (Guard Clause).
187. Напишите метод с локальной функцией `FormatResult()`, скрытой от внешнего вызова.
188. Создайте метод с Guard Clause для проверки диапазона возраста `(age < 0 || age > 150)`.
189. Реализуйте метод, замеряющий время выполнения переданного делегата (`Action`).
190. Напишите метод `Retry`, повторяющий выполнение функции до 3 раз в случае ошибки.
191. Создайте метод с мемоизацией (кэшированием) результатов вычислений.
192. Реализуйте метод безопасного приведения типов `SafeCast<T>`, возвращающий `default(T)`, если приведение не удалось.
193. Напишите метод глубокого копирования массива с предварительной проверкой на `null`.
194. Создайте метод хэширования строки, предварительно проверив её на пустоту.
195. Реализуйте метод чтения конфигурации с дефолтными значениями (если ключа нет).
196. Напишите метод логгирования, который внутри использует локальную функцию для добавления временной метки.
197. Создайте метод, оборачивающий выполнение кода в блок `try-catch` и возвращающий `bool`.
198. Реализуйте метод `Assert`, выбрасывающий исключение, если условие ложно.
199. Напишите метод валидации пользователя (проверка имени, возраста, пароля) с несколькими Guard Clauses подряд.
200. Создайте метод расчета сложной формулы, где промежуточные шаги реализованы через 3 разные локальные функции.

## Решения - 13, 22, 43, 81, 82, 101, 122, 146, 161, 186

```csharp
using System;

namespace CSharpMethodsPractice
{
    public static class Solutions
    {
        // 13. Метод, определяющий, является ли число простым
        public static bool IsPrime(int number)
        {
            if (number <= 1) return false;
            if (number == 2) return true;
            if (number % 2 == 0) return false;

            var boundary = (int)Math.Floor(Math.Sqrt(number));
            for (int i = 3; i <= boundary; i += 2)
            {
                if (number % i == 0)
                    return false;
            }
            return true;
        }

        // 22. Метод, который переворачивает строку
        public static string ReverseString(string input)
        {
            if (string.IsNullOrEmpty(input)) return input;
            char[] charArray = input.ToCharArray();
            Array.Reverse(charArray);
            return new string(charArray);
        }

        // 43. Метод поиска максимального элемента в массиве
        public static int GetMax(int[] array)
        {
            if (array == null || array.Length == 0)
                throw new ArgumentException("Массив не может быть пустым.");
            
            int max = array;
            for (int i = 1; i < array.Length; i++)
            {
                if (array[i] > max)
                    max = array[i];
            }
            return max;
        }

        // 81. Метод Swap, меняющий местами два числа через ref
        public static void Swap(ref int a, ref int b)
        {
            int temp = a;
            a = b;
            b = temp;
        }

        // 82. Метод деления, возвращающий частное и остаток через out
        public static void Divide(int dividend, int divisor, out int quotient, out int remainder)
        {
            quotient = dividend / divisor;
            remainder = dividend % divisor;
        }

        // 101. Метод приветствия с необязательным параметром имени
        public static string Greet(string name = "Гость")
        {
            return $"Здравствуйте, {name}!";
        }

        // 122. Рекурсивный метод для нахождения N-го числа Фибоначчи
        public static int Fibonacci(int n)
        {
            if (n < 0) throw new ArgumentException("N не может быть отрицательным");
            if (n == 0) return 0;
            if (n == 1) return 1;
            
            return Fibonacci(n - 1) + Fibonacci(n - 2);
        }

        // 161. Метод, возвращающий минимальное и максимальное значение массива в виде кортежа
        public static (int Min, int Max) GetMinMax(int[] array)
        {
            if (array == null || array.Length == 0)
                throw new ArgumentException("Массив пуст");

            int min = array;
            int max = array;

            foreach (int item in array)
            {
                if (item < min) min = item;
                if (item > max) max = item;
            }

            return (min, max);
        }

        // 186. Безопасное деление, проверяющее знаменатель на 0 (Guard Clause)
        public static double SafeDivide(double numerator, double denominator)
        {
            // Guard Clause
            if (denominator == 0)
                throw new DivideByZeroException("Знаменатель не может быть равен нулю.");

            return numerator / denominator;
        }
    }

    // Класс для методов расширения
    public static class StringExtensions
    {
        // 146. Метод расширения для string, считающий количество слов
        public static int WordCount(this string text)
        {
            if (string.IsNullOrWhiteSpace(text))
                return 0;

            string[] words = text.Split(new char[] { ' ', '.', '?', '!', '\n', '\r', '\t' }, 
                                        StringSplitOptions.RemoveEmptyEntries);
            return words.Length;
        }
    }
}
```
